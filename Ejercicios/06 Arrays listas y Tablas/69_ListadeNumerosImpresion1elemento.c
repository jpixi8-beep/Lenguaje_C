//Lista de numeros
#include <stdio.h>

int main() {
    int numeros[]= {10, 20, 30, 40, 50}; 
    printf("%d\n", numeros[0]); // Imprime el primer número del arreglo (10)

    printf("%d\n", numeros[1]); // Imprime el segundo número del arreglo (20)
    printf("%d\n", numeros[2]); // Imprime el tercer número del arreglo (30)
    printf("%d\n", numeros[3]); // Imprime el cuarto número del arreglo (40)
    printf("%d\n", numeros[4]); // Imprime el quinto número del arreglo (50)
    return 0;
}